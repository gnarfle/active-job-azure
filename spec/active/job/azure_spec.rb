# frozen_string_literal: true

RSpec.describe Active::Job::Azure do
  it "has a version number" do
    expect(Active::Job::Azure::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
